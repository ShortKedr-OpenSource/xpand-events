namespace Xpand.Events {
    public static class XpandEventsConfig {
        
        internal const int DefaultSubscriptionsBuffer = 8;
        
        public static LogLevel LogLevel = LogLevel.None;
    }
}